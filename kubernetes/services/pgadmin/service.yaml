---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: pgadmin
  name: pgadmin
  namespace: hpc-services
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 5100
  selector:
    app: pgadmin
  type: LoadBalancer